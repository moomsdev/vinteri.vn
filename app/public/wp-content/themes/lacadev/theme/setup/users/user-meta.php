<?php
/**
 * Post Meta.
 *
 * Here, you can register custom post meta fields using the Carbon Fields library.
 *
 * @link https://carbonfields.net/docs/containers-post-meta/
 *
 * @package WPEmergeCli
 */

use Carbon_Fields\Container\Container;
use Carbon_Fields\Field\Field;

// phpcs:disable

// Container::make( 'user_meta', __( 'Custom Data', 'laca' ) )
// 	->add_fields( array(
// 		Field::make( 'image', 'crb_img' ),
//         Field::make('checkbox', 'is_feature', __('Nổi bật', 'laca'))->set_default_value(false),
// 	));

// phpcs:enable
